services:
  # Frontend container
  frontend:
    build: ./frontend
    ports:
      - "8080:3000"
    volumes:
      # Legacy Dockerfile mismatch:
      # - ./frontend:/usr/src/app
      # - /usr/src/app/node_modules
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev
    depends_on:
      - backend
    restart: unless-stopped
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://localhost:4000/api
    mem_limit: 512m
    cpus: 0.75

  # Backend API container
  backend:
    build: ./backend
    ports:
      - "4000:3000"
    volumes:
      # Legacy Dockerfile mismatch:
      # - ./backend:/usr/src/app
      # - /usr/src/app/node_modules
      - ./backend:/app
      - /app/node_modules
    environment:
      # Legacy placeholder:
      # - HUGGINGFACE_TOKEN=your_actual_huggingface_token_here
      - HUGGINGFACE_TOKEN=${HUGGINGFACE_TOKEN}
      - JWT_SECRET=${JWT_SECRET:-brainbytes-local-secret}
      - NODE_ENV=development
      - MONGO_URI=mongodb://mongo:27017/brainbytes
    command: npm run dev
    mem_limit: 512m
    cpus: 0.75
    depends_on:
      - mongo
    restart: unless-stopped

  # MongoDB container
  mongo:
    image: mongo:4.4
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped
    mem_limit: 512m
    cpus: 0.75

# Define volumes for persistent data
volumes:
  mongo-data:
